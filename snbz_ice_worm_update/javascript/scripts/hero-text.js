/**
 * 
 * UWE Subnautica Expansion - Hero Text Script
 * Author - hugh@unknownworlds.com
 * 
 */

 const HERO_TEXT = new HeroText(
     'hero-text', // The ID of the hero text element
     0.1, // A margin the user must scroll past before the text will animate, as
     // a percentage of the viewport height. 1 = 100%.
     0.6 // The point at which the text will be fully visible, as a percentage
     // of viewport height. 1 = 100%
    );
